var reversePairs = function (record) {
  mergeNumber(record, 0, record.length);
};

/**
 * 归并排序
 * @param {number[]} nums
 * @param {number} left
 * @param {number} right
 */
function mergeNumber(nums, left, right) {
    if(){}
}
